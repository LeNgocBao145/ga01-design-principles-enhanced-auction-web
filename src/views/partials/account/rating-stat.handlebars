<div class="col-md-4 mb-3 mb-md-0">
  <div class="stat-card text-center">
    <div class="stat-icon {{color}} mx-auto">
      <i class="bi {{icon}}"></i>
    </div>
    <h3 class="fw-bold mb-1">{{value}}</h3>
    <small class="text-muted">{{title}}</small>
  </div>
</div>